// xnqsbar config.hxx
// .xnqs 2023

// change the order of the elements in this array or completely remove some to 
// example: const int enum_order[] = {KRNL, RAM, TIME, DATE};
const int enum_order[] = {KRNL, CPU, RAM, SWAP, DATE, TIME};

// delay in microseconds inbetween each update
const int DELAY = 1000000;
